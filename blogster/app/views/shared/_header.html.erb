<header>
  <nav class="navbar navbar-inverse navbar-fixed-top bg-faded">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
          data-target="#bs-example-navbar-collapse-1" aria-expanded="false"
          >
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "soundster", root_path, class: "navbar-brand" %>
      </div>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li class="nav-item <%= "active" if current_page?(root_path)%>">
            <%= link_to "Home", root_path %>
          </li>
          <% Category.all.each do |category| %>
            <li class="nav-item <%= "active" if current_page?(posts_path(category: category.name)) %>">
              <%= link_to category.name, posts_path(category: category.name) %>
            </li>
          <% end %>
        </ul>
        <%= render 'shared/buttons/create' %>
      </div>
    </div>
  </nav>
</header>